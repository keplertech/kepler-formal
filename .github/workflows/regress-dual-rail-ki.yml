# GitHub Actions workflow to run SEC dual-rail k-induction regressions.
name: regress-dual-rail-ki

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/regress-ki.yml
    with:
      sec_encoding: dual_rail_steady
      positive_expectation: allow-inconclusive
