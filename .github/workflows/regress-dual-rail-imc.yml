# GitHub Actions workflow to run SEC dual-rail IMC regressions.
name: regress-dual-rail-imc

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/regress-imc.yml
    with:
      sec_encoding: dual_rail_steady
      positive_expectation: allow-inconclusive
