# GitHub Actions workflow to run SEC dual-rail PDR regressions.
name: regress-dual-rail-pdr

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/regress-pdr.yml
    with:
      sec_encoding: dual_rail_steady
      require_full_coverage: true
